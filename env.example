# MAICE 시스템 환경변수 설정 예시
# 이 파일을 .env로 복사하고 실제 값으로 수정하세요

# 데이터베이스 설정
DATABASE_URL=postgresql+asyncpg://maice_user:maice_password@localhost:5432/maice_db

# JWT 설정
SECRET_KEY=your-secret-key-here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Redis 설정
REDIS_URL=redis://localhost:6379

# OpenAI 설정
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_CHAT_MODEL=gpt-5-mini
OPENAI_MAX_TOKENS=4000
OPENAI_TEMPERATURE=0.7

# LLM Provider 설정 (openai, anthropic, google, mcp)
LLM_PROVIDER=mcp

# Anthropic Claude 설정
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here
ANTHROPIC_CHAT_MODEL=claude-sonnet-4-20250514

# Google Gemini 설정
GOOGLE_CHAT_MODEL=gemini-2.5-flash-lite
GEMINI_API_KEY=AIzaSy-your-google-api-key-here

# MCP 서버 설정
# OpenAI 호환 API 사용 시 (권장)
MCP_OPENAI_BASE_URL=http://10.100.2.3:5555/v1
MCP_API_KEY=your-mcp-api-key-here
MCP_MODEL=penGPT

# 커스텀 엔드포인트 사용 시 (기존 방식)
# MCP_SERVER_URL=http://192.168.1.105:5555

# Google OAuth 설정 (Jenkins에서 주입)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://your-domain.com/auth/google/callback

# 관리자 설정 (Jenkins에서 주입)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-admin-password
SESSION_SECRET_KEY=your-session-secret-key

# 애플리케이션 설정
DEBUG=True
HOST=0.0.0.0
PORT=8000

# 환경 설정
ENVIRONMENT=development
ENABLE_MAICE_TEST=true

# 에이전트 설정
ORCHESTRATOR_MODE=decentralized
FORCE_NON_STREAMING=1
AUTO_PROMOTE_AFTER_CLARIFICATION=0

# 로깅 제어 (기본 비활성화)
# LLM 프롬프트/응답 원문 로깅은 민감정보 노출 가능성이 있어 기본적으로 비활성화합니다.
# 필요 시 true/1/yes 로 설정하세요.
MAICE_LOG_LLM_PROMPTS=false
MAICE_LOG_LLM_RESPONSES=false
