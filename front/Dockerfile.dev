# 개발용 프론트엔드 Dockerfile
FROM node:22-alpine

# 작업 디렉토리 설정
WORKDIR /app

# package.json과 yarn.lock 복사 (의존성 캐싱을 위해)
COPY package.json yarn.lock ./

# 의존성 설치
RUN yarn install

# 소스 코드 복사
COPY . .

# 개발 서버 포트 노출
EXPOSE 3000

# 개발 서버 실행 (Hot Reload 지원)
CMD ["yarn", "dev", "--host", "0.0.0.0"]
